# Vercel KV — for persisting group vote sessions across requests
# Get these from your Vercel project dashboard after adding KV storage
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Without these, vote sessions use in-memory storage (resets on restart — fine for local dev)
